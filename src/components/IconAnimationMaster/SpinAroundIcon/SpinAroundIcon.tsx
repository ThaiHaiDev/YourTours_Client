import './SpinAroundIcon.scss';

const SpinAroundIcon = () => {
    return (
        <div className="spin-around">
            <h1 className="ring">{}</h1>
        </div>
    );
};

export default SpinAroundIcon;
